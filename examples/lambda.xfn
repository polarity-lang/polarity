data Nat {
    Z : Nat,
    S(n: Nat) : Nat
}

impl Nat {
    def Nat.add(y: Nat) : Nat {
        Z => y,
        S(x') => S(x'.add(y)),
    }
}

data Exp {
    Var(n: Nat) : Exp,
    Lam(body: Exp) : Exp,
    App(lhs: Exp, rhs: Exp) : Exp
}
