use "./char.pol"

/// An opaque Unicode string.
extern String: Type

/// Concatenation of two strings.
extern concat(x y: String): String

/// Prepending a single unicode character to a string.
extern prepend_char(c: Char, s: String): String
