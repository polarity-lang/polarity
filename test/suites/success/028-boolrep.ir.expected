use "../../../std/data/bool.pol"

def x0.extract(x) {
    TrueRep => T,
    FalseRep => F
}

def x0.flipRep(x, rep) {
    Unit =>
        rep.match {
            TrueRep => FalseRep,
            FalseRep => TrueRep
        }
}

def x.example { Unit => Unit.flipRep(T, TrueRep).extract(F) }
