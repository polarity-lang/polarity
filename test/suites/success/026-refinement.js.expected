"use strict";
const __fs = require("node:fs");
function add_i64(x, y) {
    return BigInt.asIntN(64, x + y);
}
function sub_i64(x, y) {
    return BigInt.asIntN(64, x - y);
}
function mul_i64(x, y) {
    return BigInt.asIntN(64, x * y);
}
function div_i64(x, y) {
    return BigInt.asIntN(64, x / y);
}
function add_f64(x, y) {
    return x + y;
}
function sub_f64(x, y) {
    return x - y;
}
function mul_f64(x, y) {
    return x * y;
}
function div_f64(x, y) {
    return x / y;
}
function concat(s1, s2) {
    return s1.concat(s2);
}
function append_char(c, s) {
    return s.concat(String.fromCodePoint(c));
}
function unit() {
    return void 0;
}
function return_io(x) {
    return function () {
        return x;
    };
}
function println(s) {
    return function () {
        console.log(s);
        return void 0;
    };
}
function read_file(path) {
    return function () {
        try {
            const data = __fs.readFileSync(path, "utf8");
            return {
                tag: "Some",
                args: [data],
            };
        }
        catch (err) {
            return {
                tag: "None",
                args: [],
            };
        }
    };
}
function sym(__self, x, y) {
    switch(__self.tag){
        case "Refl":
            const x0 = __self.args[0];
            return {
                tag: "Refl",
                args: [
                    y
                ]
            };
    }
}
function subst(__self, x1, y0, p) {
    switch(__self.tag){
        case "Refl":
            const x2 = __self.args[0];
            return p;
    }
}
function trans(__self, x3, y1, z, h) {
    switch(__self.tag){
        case "Refl":
            const x4 = __self.args[0];
            return h;
    }
}
function cong(__self, x5, y2, f) {
    switch(__self.tag){
        case "Refl":
            const x6 = __self.args[0];
            return {
                tag: "Refl",
                args: [
                    f.ap0(y2)
                ]
            };
    }
}
function not(__self) {
    switch(__self.tag){
        case "True":
            return {
                tag: "False",
                args: []
            };
        case "False":
            return {
                tag: "True",
                args: []
            };
    }
}
function not_inverse(__self) {
    switch(__self.tag){
        case "True":
            return {
                tag: "Refl",
                args: [
                    {
                        tag: "True",
                        args: []
                    }
                ]
            };
        case "False":
            return {
                tag: "Refl",
                args: [
                    {
                        tag: "False",
                        args: []
                    }
                ]
            };
    }
}
