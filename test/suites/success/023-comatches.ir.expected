def x.id { Unit => comatch { .pi_elim(x0, a) => comatch { .ap(x3) => x3 } } }

def x.const {
    Unit =>
        comatch {
            .pi_elim(x0, a) =>
                comatch { .pi_elim(x2, b) => comatch { .ap(x5) => comatch { .ap(y) => x5 } } }
        }
}

codef IdType { .ap(a) => <ZST> }
