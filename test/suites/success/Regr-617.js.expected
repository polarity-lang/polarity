function eval(__self) {
    switch(__self.tag){
        case "Lit":
            const n = __self.args[0];
            return n;
        case "Add":
            const l = __self.args[0];
            const r = __self.args[1];
            return BigInt.asIntN(64, eval(l) + eval(r));
    }
}
